[build]
builder = "NIXPACKS"

[deploy]
startCommand = "php artisan storage:link || true && php artisan optimize:clear || true && php -S 0.0.0.0:$PORT -t public"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
